#!/usr/bin/env bash
# Backward-compatible name. New command: agent-switch {cc|codex} ...
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ "${1:-}" = cc ]; then
  exec "$ROOT/agent-switch" "$@"
fi
exec "$ROOT/agent-switch" codex "$@"
